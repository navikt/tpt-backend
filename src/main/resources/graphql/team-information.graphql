query TeamInformation($teamFirst: Int = 200, $teamAfter: Cursor) {
  teams(
    first: $teamFirst,
    after: $teamAfter,
    filter: {hasWorkloads: true}
  ) {
    pageInfo {
      hasNextPage
      endCursor
    }
    nodes {
      slug
      slackChannel
    }
  }
}